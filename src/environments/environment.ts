// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase:{
    apiKey: "AIzaSyD-vkbI3AtahjXliNJ0yxZjZ0T0SPKofhk",
  authDomain: "somiaaadil-31292.firebaseapp.com",
  projectId: "somiaaadil-31292",
  databaseURL: "https://somiaaadil-31292-default-rtdb.firebaseio.com",

  storageBucket: "somiaaadil-31292.appspot.com",
  messagingSenderId: "99032878005",
  appId: "1:99032878005:web:2e5b9bbc8a307a6432e7fc",
  measurementId: "G-5ZMBVL6PC2"
  }
};

/*


import { initializeApp } from 'firebase/app';

// TODO: Replace the following with your app's Firebase project configuration
const firebaseConfig = {
  //...
};

const app = initializeApp(firebaseConfig);
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
